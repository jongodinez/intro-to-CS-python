print('Calculate the base and height of a triangle!')
base = input('Enter the base of the triangle: ')
height = input('Enter the height of the triangle: ')

area = int(base) * int(height)

print(area, 'is the area of the triangle.')